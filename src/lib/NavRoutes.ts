import { base } from '$app/paths';
const routes = [
	{
		href: base + '/',
		label: 'Home'
	},
	{
		href: base + '/desafios',
		label: 'Desafíos'
	},
	{
		href: base + '/rpa',
		label: 'RPA'
	}
];

export default routes;
